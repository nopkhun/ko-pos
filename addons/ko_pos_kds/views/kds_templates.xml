<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="kds_page" name="KDS Screen">
        <html>
            <head>
                <meta charset="utf-8"/>
                <!-- Pinch zoom stays off on purpose: this is a kiosk that greasy
                     hands brush against all shift. WCAG 1.4.4 is met instead by the
                     ก- / ก+ control in the header, which is easier for staff to
                     find and, unlike a pinch, survives a reload. -->
                <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no"/>
                <meta name="theme-color" content="#0d7d88"/>
                <title>จอครัว · KO KDS</title>
                <!-- web.assets_frontend starts Odoo's asset watchdog. A raw standalone
                     page must seed the same session info as web.frontend_layout;
                     otherwise every bundle_changed event compares against an empty
                     server version and shows a false "page out of date" warning. -->
                <script type="text/javascript">
                    var odoo = {
                        csrf_token: "<t t-esc="request.csrf_token(None)"/>",
                        debug: "",
                    };
                    odoo.__session_info__ = <t t-out="json.dumps(request.env['ir.http'].get_frontend_session_info())"/>;
                </script>
                <t t-call-assets="web.assets_frontend"/>
                <style>
                    @font-face {
                        font-family: "Prompt";
                        src: url("/ko_pos_ui/static/src/fonts/Prompt-Regular.ttf") format("truetype");
                        font-weight: 400;
                        font-display: swap;
                    }
                    @font-face {
                        font-family: "Prompt";
                        src: url("/ko_pos_ui/static/src/fonts/Prompt-Medium.ttf") format("truetype");
                        font-weight: 500;
                        font-display: swap;
                    }
                    @font-face {
                        font-family: "Prompt";
                        src: url("/ko_pos_ui/static/src/fonts/Prompt-SemiBold.ttf") format("truetype");
                        font-weight: 600;
                        font-display: swap;
                    }
                    @font-face {
                        font-family: "Prompt";
                        src: url("/ko_pos_ui/static/src/fonts/Prompt-Bold.ttf") format("truetype");
                        font-weight: 700;
                        font-display: swap;
                    }

                    /* ------------------------------------------------------------------
                       Design notes for whoever edits this next
                       ------------------------------------------------------------------
                       The board is read at roughly 50 cm on a counter tablet, and at
                       arm's length on a phone, by someone whose hands are wet, gloved,
                       or holding a pan. Three rules follow from that and they are the
                       reason the numbers below look large:

                       1. Nothing that can be tapped is smaller than --tap (56 px).
                          44 px is the bare-finger minimum; a gloved finger needs more.
                       2. Every piece of state is carried by at least two channels —
                          colour AND a word AND a shape. Colour alone fails for the
                          ~8 % of men with a colour vision deficiency, and it fails for
                          everyone under a steamy kitchen light.
                       3. The special instruction ("ไม่ใส่ผัก") is the text that costs a
                          remake when it is missed, so it is the loudest text on the
                          card, not the faintest. It used to be 12 px grey.

                       --s is the staff-adjustable text scale (ก- / ก+ in the header,
                       persisted in localStorage). Express every font-size through it.
                       ------------------------------------------------------------------ */
                    :root {
                        --s: 1;

                        --ko-teal: #0d7d88;
                        --ko-teal-dark: #08616a;
                        --ko-teal-soft: #e4f2f3;

                        --ko-ok: #14794f;
                        --ko-ok-soft: #e2f4ea;
                        --ko-ok-line: #1f9c68;

                        --ko-warn: #9a5600;
                        --ko-warn-soft: #fff1d9;
                        --ko-warn-line: #e08a00;

                        --ko-danger: #b3122f;
                        --ko-danger-soft: #ffe7eb;
                        --ko-danger-line: #d92c4c;

                        --ko-ink: #101a1c;
                        --ko-ink-2: #3d4f52;
                        --ko-ink-3: #5d7175;
                        --ko-border: #cfdcdb;
                        --ko-canvas: #eef3f3;
                        --ko-surface: #ffffff;

                        --tap: 56px;
                        --radius: 14px;
                    }

                    * { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }

                    html { -webkit-text-size-adjust: 100%; }

                    body {
                        font-family: "Prompt", -apple-system, "Noto Sans Thai", sans-serif;
                        color: var(--ko-ink);
                        background: var(--ko-canvas);
                        height: 100dvh;
                        display: flex;
                        flex-direction: column;
                        overflow: hidden;
                        font-size: calc(16px * var(--s));
                    }

                    button { font-family: inherit; border: 0; background: none; color: inherit; cursor: pointer; }
                    button:focus-visible, a:focus-visible { outline: 3px solid var(--ko-teal); outline-offset: 2px; }

                    /* ============================ header ============================ */

                    header {
                        flex: none;
                        background: var(--ko-surface);
                        border-bottom: 1px solid var(--ko-border);
                        box-shadow: 0 1px 0 rgba(16, 26, 28, 0.04);
                    }

                    .hd-top {
                        display: flex;
                        align-items: center;
                        gap: 10px;
                        padding: 10px 14px 8px;
                    }

                    .hd-title {
                        font-size: calc(19px * var(--s));
                        font-weight: 700;
                        flex: none;
                    }

                    .hd-scope {
                        display: flex;
                        align-items: center;
                        gap: 6px;
                        flex: 1;
                        min-width: 0;
                        overflow: hidden;
                    }

                    .hd-chip {
                        font-size: calc(14px * var(--s));
                        font-weight: 600;
                        color: var(--ko-teal-dark);
                        background: var(--ko-teal-soft);
                        border-radius: 999px;
                        padding: 3px 11px;
                        white-space: nowrap;
                        overflow: hidden;
                        text-overflow: ellipsis;
                    }

                    .hd-chip.muted { color: var(--ko-ink-3); background: #eef2f2; }

                    .hd-right { display: flex; align-items: center; gap: 8px; flex: none; }

                    /* Text size control. Kitchens differ by a metre or two and by how
                       good the cook's eyes are; a fixed size cannot serve both. */
                    .zoom { display: flex; background: #eef2f2; border-radius: 999px; padding: 2px; }

                    .zoom button {
                        width: 46px;
                        height: 46px;
                        border-radius: 999px;
                        font-weight: 700;
                        color: var(--ko-ink-2);
                        line-height: 1;
                    }

                    .zoom button:active { background: #dfe7e7; }
                    .zoom button[disabled] { opacity: 0.35; cursor: default; }
                    .zoom .z-sm { font-size: 14px; }
                    .zoom .z-lg { font-size: 21px; }

                    .clock {
                        font-size: calc(17px * var(--s));
                        font-weight: 700;
                        font-variant-numeric: tabular-nums;
                        white-space: nowrap;
                    }

                    /* --------- tabs: the three boards --------- */

                    .tabs { display: flex; gap: 8px; padding: 0 14px 10px; }

                    .tab {
                        flex: 1;
                        min-height: var(--tap);
                        border-radius: 12px;
                        font-size: calc(16px * var(--s));
                        font-weight: 600;
                        color: var(--ko-ink-2);
                        background: #eef2f2;
                        display: flex;
                        align-items: center;
                        justify-content: center;
                        gap: 7px;
                        transition: background 120ms ease, color 120ms ease;
                    }

                    .tab:active { transform: scale(0.98); }
                    .tab.on { color: #fff; background: var(--ko-teal); }

                    .tab .n {
                        font-weight: 700;
                        font-variant-numeric: tabular-nums;
                        background: rgba(16, 26, 28, 0.08);
                        border-radius: 999px;
                        padding: 1px 9px;
                        min-width: 30px;
                    }

                    .tab.on .n { background: rgba(255, 255, 255, 0.24); }

                    /* --------- filters row --------- */

                    .filters {
                        display: flex;
                        align-items: center;
                        gap: 8px;
                        padding: 0 14px 10px;
                        overflow-x: auto;
                        scrollbar-width: none;
                        white-space: nowrap;
                    }

                    .filters::-webkit-scrollbar { display: none; }

                    .seg { display: flex; background: #eef2f2; border-radius: 12px; padding: 3px; flex: none; }

                    .seg button {
                        min-height: 46px;
                        padding: 0 16px;
                        border-radius: 10px;
                        font-size: calc(15px * var(--s));
                        font-weight: 600;
                        color: var(--ko-ink-3);
                    }

                    .seg button.on {
                        color: var(--ko-teal-dark);
                        background: #fff;
                        box-shadow: 0 1px 3px rgba(16, 26, 28, 0.12);
                    }

                    .f-label {
                        font-size: calc(14px * var(--s));
                        font-weight: 600;
                        color: var(--ko-ink-3);
                        flex: none;
                        padding-left: 4px;
                    }

                    .f-chip {
                        flex: none;
                        display: inline-flex;
                        align-items: center;
                        min-height: 46px;
                        padding: 0 16px;
                        border-radius: 999px;
                        font-size: calc(15px * var(--s));
                        font-weight: 600;
                        text-decoration: none;
                        border: 2px solid var(--ko-border);
                        color: var(--ko-ink-2);
                        background: #fff;
                    }

                    .f-chip.on {
                        border-color: var(--ko-teal);
                        color: #fff;
                        background: var(--ko-teal);
                    }

                    .f-sep { flex: none; width: 1px; height: 26px; background: var(--ko-border); margin: 0 4px; }

                    /* Connection warning. Before this existed a kitchen with dead wi-fi
                       kept showing a frozen board and nobody could tell. */
                    .offline {
                        display: none;
                        align-items: center;
                        gap: 8px;
                        padding: 10px 14px;
                        background: var(--ko-danger);
                        color: #fff;
                        font-size: calc(15px * var(--s));
                        font-weight: 600;
                    }

                    body.is-offline .offline { display: flex; }

                    /* ============================ board ============================ */

                    main {
                        flex: 1;
                        overflow-y: auto;
                        -webkit-overflow-scrolling: touch;
                        min-height: 0;
                        padding: 12px 14px calc(24px + env(safe-area-inset-bottom));
                        background: var(--ko-canvas);
                    }

                    .sec { margin-bottom: 18px; }
                    .sec:last-child { margin-bottom: 0; }

                    .sec-hd {
                        position: sticky;
                        top: -12px;
                        z-index: 5;
                        display: flex;
                        align-items: center;
                        gap: 9px;
                        margin: 0 -14px 10px;
                        padding: 8px 14px;
                        font-size: calc(15px * var(--s));
                        font-weight: 700;
                        color: var(--ko-ink-2);
                        background: var(--ko-canvas);
                    }

                    .sec-hd .dot { width: 11px; height: 11px; border-radius: 50%; background: var(--ko-teal); flex: none; }
                    .sec-ready .sec-hd { color: var(--ko-ok); }
                    .sec-ready .sec-hd .dot { background: var(--ko-ok-line); }

                    .sec-hd .n {
                        font-variant-numeric: tabular-nums;
                        background: #fff;
                        border: 1px solid var(--ko-border);
                        border-radius: 999px;
                        padding: 1px 10px;
                    }

                    .grid {
                        display: grid;
                        grid-template-columns: repeat(auto-fill, minmax(min(100%, 340px), 1fr));
                        gap: 12px;
                        align-content: start;
                    }

                    .grid.compact { grid-template-columns: repeat(auto-fill, minmax(min(100%, 260px), 1fr)); }

                    .empty {
                        grid-column: 1 / -1;
                        padding: 56px 20px;
                        text-align: center;
                        color: var(--ko-ink-3);
                        font-size: calc(17px * var(--s));
                        font-weight: 500;
                    }

                    /* ============================ ticket card ============================ */

                    .tk {
                        background: var(--ko-surface);
                        border-radius: var(--radius);
                        border: 1px solid var(--ko-border);
                        overflow: hidden;
                        display: flex;
                        flex-direction: column;
                        box-shadow: 0 1px 2px rgba(16, 26, 28, 0.05);
                    }

                    /* The whole head band carries urgency, not a 4 px hairline. At a
                       glance from the pass you should be able to say "that one is late"
                       without reading anything. */
                    .tk-head {
                        display: flex;
                        align-items: center;
                        gap: 10px;
                        padding: 11px 14px;
                        background: var(--ko-teal-soft);
                        border-bottom: 1px solid var(--ko-border);
                        color: var(--ko-teal-dark);
                    }

                    .tk.warn .tk-head { background: var(--ko-warn-soft); color: var(--ko-warn); }
                    .tk.late .tk-head { background: var(--ko-danger); color: #fff; border-bottom-color: var(--ko-danger); }
                    .tk.rdy .tk-head { background: var(--ko-ok-soft); color: var(--ko-ok); }
                    .tk.dead .tk-head { background: #eef2f2; color: var(--ko-ink-3); }

                    .tk-id { flex: 1; min-width: 0; }

                    .tk-where {
                        display: block;
                        font-size: calc(20px * var(--s));
                        font-weight: 700;
                        line-height: 1.25;
                        overflow: hidden;
                        text-overflow: ellipsis;
                        white-space: nowrap;
                    }

                    .tk-sub {
                        display: block;
                        font-size: calc(13px * var(--s));
                        font-weight: 500;
                        opacity: 0.85;
                        font-variant-numeric: tabular-nums;
                    }

                    .tk-time { flex: none; text-align: right; line-height: 1; }

                    .tk-min {
                        font-size: calc(26px * var(--s));
                        font-weight: 700;
                        font-variant-numeric: tabular-nums;
                        display: block;
                    }

                    .tk-min-u { font-size: calc(12px * var(--s)); font-weight: 600; opacity: 0.85; }

                    .tk-flags { display: flex; flex-wrap: wrap; gap: 5px; padding: 0 14px; margin-top: 9px; }

                    .flag {
                        font-size: calc(12.5px * var(--s));
                        font-weight: 700;
                        border-radius: 6px;
                        padding: 3px 9px;
                        letter-spacing: 0.2px;
                    }

                    .flag-remake { color: #fff; background: var(--ko-danger); }
                    .flag-paid { color: var(--ko-ok); background: var(--ko-ok-soft); border: 1px solid var(--ko-ok-line); }
                    .flag-away { color: var(--ko-teal-dark); background: var(--ko-teal-soft); border: 1px solid var(--ko-teal); }

                    .tk-sla { height: 6px; background: #e7eeee; }
                    .tk-sla i { display: block; height: 100%; background: var(--ko-teal); transition: width 1s linear; }
                    .tk.warn .tk-sla i { background: var(--ko-warn-line); }
                    .tk.late .tk-sla i { background: var(--ko-danger-line); }
                    .tk.rdy .tk-sla i { background: var(--ko-ok-line); }

                    .tk-lines { flex: 1; }

                    /* ---------------- one dish ---------------- */

                    .ln { display: flex; align-items: stretch; border-bottom: 1px solid #edf2f2; }
                    .ln:last-child { border-bottom: 0; }

                    /* Left: the big "I cooked this" target. Right: a separate, narrower
                       "แจ้งปัญหา" target with its own divider. They used to sit in one
                       30 px row and staff kept hitting the wrong one. */
                    .ln-tap {
                        flex: 1;
                        min-width: 0;
                        min-height: var(--tap);
                        display: flex;
                        flex-direction: column;
                        justify-content: center;
                        gap: 5px;
                        padding: 10px 12px;
                        text-align: left;
                        transition: background 90ms ease;
                    }

                    .ln-tap:active { background: var(--ko-teal-soft); }
                    .ln.is-done .ln-tap:active { background: #eef2f2; }
                    .ln.is-dead .ln-tap { cursor: default; }

                    .ln-row { display: flex; align-items: center; gap: 10px; }

                    /* Shape channel: an empty box that becomes a filled tick. */
                    .ck {
                        flex: none;
                        width: 30px;
                        height: 30px;
                        border-radius: 8px;
                        border: 2.5px solid var(--ko-border);
                        background: #fff;
                        display: flex;
                        align-items: center;
                        justify-content: center;
                        font-size: 19px;
                        font-weight: 700;
                        color: transparent;
                        line-height: 1;
                        transition: all 120ms ease;
                    }

                    .ln.is-done .ck { border-color: var(--ko-ok-line); background: var(--ko-ok-line); color: #fff; }
                    .ln.is-dead .ck { border-color: var(--ko-danger-line); background: var(--ko-danger-soft); color: var(--ko-danger); }

                    .qty {
                        flex: none;
                        min-width: 42px;
                        height: 34px;
                        padding: 0 7px;
                        border-radius: 9px;
                        background: var(--ko-ink);
                        color: #fff;
                        font-size: calc(18px * var(--s));
                        font-weight: 700;
                        font-variant-numeric: tabular-nums;
                        display: flex;
                        align-items: center;
                        justify-content: center;
                    }

                    .ln.is-done .qty { background: #b8c6c6; }
                    .ln.is-dead .qty { background: var(--ko-danger); }

                    /* The dish name owns the whole width of the row. Station and
                       status used to sit beside it and squeezed Thai names into
                       two ragged lines ("ข้าวผัด / ปู"); they now live on .ln-meta
                       underneath, where they cost nothing. */
                    .nm {
                        flex: 1;
                        min-width: 0;
                        font-size: calc(20px * var(--s));
                        font-weight: 600;
                        line-height: 1.3;
                        overflow-wrap: anywhere;
                    }

                    .ln-meta { display: flex; flex-wrap: wrap; gap: 6px; margin-left: 40px; }

                    .ln.is-done .nm { color: var(--ko-ink-3); text-decoration: line-through; text-decoration-thickness: 2px; }
                    .ln.is-dead .nm { color: var(--ko-ink-3); text-decoration: line-through; text-decoration-thickness: 2px; }

                    /* Word channel, so state never rests on colour alone. */
                    .st {
                        flex: none;
                        font-size: calc(13px * var(--s));
                        font-weight: 700;
                        border-radius: 999px;
                        padding: 3px 10px;
                    }

                    .st-done { color: var(--ko-ok); background: var(--ko-ok-soft); }
                    .st-dead { color: var(--ko-danger); background: var(--ko-danger-soft); }

                    /* Station name only earns its space on the "ทั้งหมด" board — when one
                       station is selected every line would say the same thing. */
                    .stn {
                        flex: none;
                        font-size: calc(13px * var(--s));
                        font-weight: 700;
                        padding: 3px 9px;
                        border-radius: 7px;
                        background: #f2f6f6;
                    }

                    .stn.none {
                        color: var(--ko-warn);
                        background: var(--ko-warn-soft);
                        border: 1px dashed var(--ko-warn-line);
                    }

                    /* The line that causes remakes when it is missed. Loud on purpose. */
                    .ln-note {
                        display: flex;
                        gap: 7px;
                        margin-left: 40px;
                        padding: 7px 11px;
                        border-radius: 9px;
                        border-left: 4px solid var(--ko-warn-line);
                        background: var(--ko-warn-soft);
                        color: var(--ko-warn);
                        font-size: calc(16px * var(--s));
                        font-weight: 600;
                        line-height: 1.35;
                        text-align: left;
                    }

                    .ln-note b { font-weight: 700; }

                    .tk-note {
                        display: flex;
                        gap: 7px;
                        margin: 10px 14px 0;
                        padding: 9px 12px;
                        border-radius: 9px;
                        border-left: 4px solid var(--ko-warn-line);
                        background: var(--ko-warn-soft);
                        color: var(--ko-warn);
                        font-size: calc(16px * var(--s));
                        font-weight: 600;
                        line-height: 1.35;
                    }

                    .ln-flag {
                        flex: none;
                        width: 64px;
                        border-left: 1px solid #edf2f2;
                        display: flex;
                        flex-direction: column;
                        align-items: center;
                        justify-content: center;
                        gap: 2px;
                        font-size: calc(11.5px * var(--s));
                        font-weight: 700;
                        color: var(--ko-danger);
                        line-height: 1.15;
                    }

                    .ln-flag .ic { font-size: 20px; line-height: 1; }
                    .ln-flag:active { background: var(--ko-danger-soft); }

                    /* ---------------- issue reported by the kitchen ---------------- */

                    .ln-issue {
                        display: flex;
                        align-items: center;
                        gap: 10px;
                        margin: 0 12px 10px 40px;
                        padding: 9px 12px;
                        border-radius: 9px;
                        border: 2px solid var(--ko-danger-line);
                        background: var(--ko-danger-soft);
                        color: var(--ko-danger);
                        font-size: calc(15px * var(--s));
                        font-weight: 600;
                        line-height: 1.35;
                    }

                    .ln-issue.acked {
                        border-color: var(--ko-border);
                        background: #f2f6f6;
                        color: var(--ko-ink-2);
                    }

                    .ln-issue .txt { flex: 1; min-width: 0; }
                    .ln-issue .sub { display: block; font-weight: 500; font-size: calc(13px * var(--s)); opacity: 0.9; }

                    .ln-issue button {
                        flex: none;
                        min-height: 44px;
                        padding: 0 14px;
                        border-radius: 9px;
                        border: 2px solid currentColor;
                        font-size: calc(13.5px * var(--s));
                        font-weight: 700;
                    }

                    /* ---------------- card action ---------------- */

                    .tk-act {
                        margin: 12px;
                        min-height: 60px;
                        border-radius: 12px;
                        font-size: calc(18px * var(--s));
                        font-weight: 700;
                        display: flex;
                        align-items: center;
                        justify-content: center;
                        gap: 8px;
                        transition: transform 90ms ease;
                    }

                    .tk-act:active { transform: scale(0.98); }
                    .act-start { color: #fff; background: var(--ko-warn-line); }
                    .act-ready { color: #fff; background: var(--ko-ok); }
                    .act-remake { color: var(--ko-danger); background: var(--ko-danger-soft); border: 2px solid var(--ko-danger-line); }

                    .act-wait {
                        color: var(--ko-ok);
                        background: var(--ko-ok-soft);
                        border: 2px dashed var(--ko-ok-line);
                        cursor: default;
                    }

                    /* A second tap confirms anything that cannot be undone. */
                    .tk-act.armed {
                        color: #fff;
                        background: var(--ko-danger);
                        border-color: var(--ko-danger);
                        animation: nudge 600ms ease infinite alternate;
                    }

                    @keyframes nudge { from { opacity: 1; } to { opacity: 0.78; } }

                    /* ---------------- ready strip ---------------- */

                    .rdy-dishes {
                        padding: 10px 14px 12px;
                        font-size: calc(16px * var(--s));
                        font-weight: 600;
                        color: var(--ko-ink-2);
                        line-height: 1.45;
                    }

                    .rdy-dishes span { white-space: nowrap; }

                    /* ---------------- menu view ---------------- */

                    .mv-total {
                        flex: none;
                        font-size: calc(30px * var(--s));
                        font-weight: 700;
                        font-variant-numeric: tabular-nums;
                    }

                    /* ============================ toast ============================ */

                    .k-toasts {
                        position: fixed;
                        left: 0;
                        right: 0;
                        bottom: calc(20px + env(safe-area-inset-bottom));
                        display: flex;
                        justify-content: center;
                        padding: 0 14px;
                        z-index: 100;
                        pointer-events: none;
                    }

                    .k-toast {
                        pointer-events: auto;
                        display: flex;
                        align-items: center;
                        gap: 12px;
                        max-width: 560px;
                        background: var(--ko-ink);
                        color: #fff;
                        font-size: calc(16px * var(--s));
                        font-weight: 600;
                        padding: 12px 12px 12px 20px;
                        border-radius: 999px;
                        box-shadow: 0 8px 28px rgba(16, 26, 28, 0.3);
                        animation: pop 180ms ease;
                    }

                    @keyframes pop { from { transform: translateY(14px); opacity: 0; } to { transform: none; opacity: 1; } }

                    .k-toast .undo {
                        flex: none;
                        min-height: 44px;
                        padding: 0 18px;
                        border-radius: 999px;
                        background: rgba(255, 255, 255, 0.18);
                        color: #fff;
                        font-size: calc(15px * var(--s));
                        font-weight: 700;
                    }

                    .k-toast .undo:active { background: rgba(255, 255, 255, 0.32); }

                    /* New dish arriving flashes the whole board once — a chime alone
                       loses to an extraction hood. */
                    body.flash main { animation: flash 900ms ease; }
                    @keyframes flash {
                        0%, 100% { background: var(--ko-canvas); }
                        30% { background: #fff3cd; }
                    }

                    /* ============================ issue sheet ============================ */

                    .sheet-bg {
                        position: fixed;
                        inset: 0;
                        background: rgba(15, 25, 27, 0.5);
                        display: none;
                        align-items: flex-end;
                        justify-content: center;
                        z-index: 200;
                    }

                    .sheet-bg.open { display: flex; }

                    .sheet {
                        width: 100%;
                        max-width: 560px;
                        background: #fff;
                        border-radius: 20px 20px 0 0;
                        padding: 20px 18px calc(22px + env(safe-area-inset-bottom));
                        box-shadow: 0 -10px 34px rgba(0, 0, 0, 0.22);
                        max-height: 92dvh;
                        overflow-y: auto;
                        animation: slideup 200ms ease;
                    }

                    @keyframes slideup { from { transform: translateY(28px); } to { transform: none; } }

                    .sheet h3 { font-size: calc(20px * var(--s)); font-weight: 700; margin-bottom: 4px; }
                    .sheet .who { font-size: calc(16px * var(--s)); font-weight: 600; color: var(--ko-teal-dark); margin-bottom: 16px; }

                    .opts { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 14px; }

                    .opt {
                        min-height: 72px;
                        border-radius: 12px;
                        border: 2px solid var(--ko-border);
                        background: #fff;
                        font-size: calc(17px * var(--s));
                        font-weight: 600;
                        color: var(--ko-ink);
                        display: flex;
                        align-items: center;
                        justify-content: center;
                        gap: 8px;
                    }

                    .opt.on { border-color: var(--ko-danger); color: var(--ko-danger); background: var(--ko-danger-soft); }
                    .opt .ic { font-size: 21px; }

                    .note-in {
                        width: 100%;
                        min-height: var(--tap);
                        padding: 14px;
                        border-radius: 12px;
                        border: 2px solid var(--ko-border);
                        font-size: calc(17px * var(--s));
                        font-family: inherit;
                        color: var(--ko-ink);
                        margin-bottom: 14px;
                    }

                    .note-in:focus { outline: none; border-color: var(--ko-teal); }

                    .sheet-act { display: flex; gap: 10px; }

                    .sheet-act button {
                        flex: 1;
                        min-height: 62px;
                        border-radius: 12px;
                        font-size: calc(18px * var(--s));
                        font-weight: 700;
                    }

                    .b-cancel { background: #eef2f2; color: var(--ko-ink-2); }
                    .b-send { background: var(--ko-danger); color: #fff; }
                    .b-send[disabled] { opacity: 0.4; cursor: not-allowed; }

                    /* ============================ bottom nav ============================ */

                    /* On a counter tablet this bar is a foot-gun: one stray touch and the
                       kitchen board becomes the sell screen. It is therefore only shown
                       on phone widths, where staff genuinely switch between the three.
                       Wider screens get the same links as small text in the header. */
                    .k-nav {
                        position: fixed;
                        left: 0;
                        right: 0;
                        bottom: 0;
                        z-index: 50;
                        display: none;
                        padding: 6px 8px calc(8px + env(safe-area-inset-bottom));
                        background: #fff;
                        border-top: 1px solid var(--ko-border);
                    }

                    .k-nav a {
                        flex: 1;
                        min-height: 52px;
                        display: flex;
                        flex-direction: column;
                        align-items: center;
                        justify-content: center;
                        color: var(--ko-ink-3);
                        text-decoration: none;
                        border-radius: 12px;
                        font-size: 15px;
                        font-weight: 600;
                    }

                    .k-nav a.on { color: var(--ko-teal-dark); background: var(--ko-teal-soft); }
                    .k-nav a small { font-size: 10px; font-weight: 400; }

                    .hd-links { display: flex; gap: 4px; flex: none; }

                    .hd-links a {
                        min-height: 44px;
                        padding: 0 14px;
                        display: flex;
                        align-items: center;
                        border-radius: 9px;
                        font-size: calc(14px * var(--s));
                        font-weight: 600;
                        color: var(--ko-ink-3);
                        text-decoration: none;
                        background: #eef2f2;
                    }

                    @media (max-width: 640px) {
                        .k-nav { display: flex; }
                        main { padding-bottom: calc(92px + env(safe-area-inset-bottom)); }
                        .k-toasts { bottom: calc(96px + env(safe-area-inset-bottom)); }
                        .hd-links { display: none; }
                        .hd-top { padding: 8px 12px 6px; gap: 8px; }
                        .hd-title { font-size: calc(16px * var(--s)); }
                        .tabs, .filters { padding-left: 12px; padding-right: 12px; }
                        main { padding-left: 12px; padding-right: 12px; }
                        .sec-hd { margin-left: -12px; margin-right: -12px; }
                        .tk-act { margin: 10px; }
                        .clock { display: none; }
                    }

                    @media (prefers-reduced-motion: reduce) {
                        * { animation: none !important; transition: none !important; }
                    }
                </style>
            </head>
            <body>
                <div id="kds-bus-bridge" hidden="hidden" t-att-data-channel="'ko_pos_kds_%s' % (pos_config.id if pos_config else 0)"/>

                <header>
                    <div class="hd-top">
                        <div class="hd-title">จอครัว</div>
                        <div class="hd-scope">
                            <span t-if="pos_config" class="hd-chip" t-esc="pos_config.display_name"/>
                            <span t-if="station" class="hd-chip" t-esc="station.name"/>
                            <span t-if="not station" class="hd-chip muted">ทุกสถานี</span>
                        </div>
                        <div class="hd-right">
                            <div class="zoom" role="group" aria-label="ขนาดตัวอักษร">
                                <button type="button" class="z-sm" id="zoom-out" aria-label="ตัวอักษรเล็กลง">ก</button>
                                <button type="button" class="z-lg" id="zoom-in" aria-label="ตัวอักษรใหญ่ขึ้น">ก</button>
                            </div>
                            <span class="clock" id="clock">--:--</span>
                            <div t-if="pos_config" class="hd-links">
                                <a t-att-href="'/pos/ui/%s' % pos_config.id">ขาย</a>
                                <a t-att-href="'/pos/ui/%s/ticket' % pos_config.id">บิล</a>
                            </div>
                        </div>
                    </div>

                    <div class="tabs" role="tablist">
                        <button type="button" class="tab on" id="tab-active" role="tab" aria-selected="true" onclick="kdsSetTab('active')">
                            กำลังทำ <span class="n" id="n-active">0</span>
                        </button>
                        <button type="button" class="tab" id="tab-served" role="tab" aria-selected="false" onclick="kdsSetTab('served')">
                            เสิร์ฟแล้ว <span class="n" id="n-served">0</span>
                        </button>
                        <button type="button" class="tab" id="tab-cancelled" role="tab" aria-selected="false" onclick="kdsSetTab('cancelled')">
                            ยกเลิก <span class="n" id="n-cancelled">0</span>
                        </button>
                    </div>

                    <div class="filters" id="filters">
                        <div class="seg" id="view-seg" role="group" aria-label="มุมมอง">
                            <button type="button" class="on" id="view-order" onclick="kdsSetView('order')">แบบออเดอร์</button>
                            <button type="button" id="view-menu" onclick="kdsSetView('menu')">แบบเมนู</button>
                        </div>

                        <!--
                          One station control only. Until 19.0.6.0.0 this page carried
                          two: hard-coded ครัวร้อน/ครัวเย็น/เครื่องดื่ม chips that filtered
                          in the browser, plus these links that filter on the server by
                          ko.kds.station. They disagreed with each other. The chips are gone.
                        -->
                        <t t-if="stations">
                            <span class="f-sep"/>
                            <span class="f-label">สถานี</span>
                            <a class="f-chip" t-att-class="'f-chip on' if not station else 'f-chip'"
                               t-att-href="'/kds/pos/%s' % pos_config.id">ทั้งหมด</a>
                            <t t-foreach="stations" t-as="stn">
                                <a class="f-chip"
                                   t-att-class="'f-chip on' if station and stn.id == station.id else 'f-chip'"
                                   t-att-href="'/kds/pos/%s?station_id=%s' % (pos_config.id, stn.id)"
                                   t-esc="stn.name"/>
                            </t>
                        </t>
                        <t t-if="len(all_configs) &gt; 1">
                            <span class="f-sep"/>
                            <span class="f-label">ร้าน</span>
                            <t t-foreach="all_configs" t-as="cfg">
                                <a class="f-chip"
                                   t-att-class="'f-chip on' if cfg.id == pos_config.id else 'f-chip'"
                                   t-att-href="'/kds/pos/%s' % cfg.id"
                                   t-esc="cfg.display_name"/>
                            </t>
                        </t>
                    </div>

                    <div class="offline" id="offline" role="status">
                        <span>⚠</span>
                        <span>ไม่ได้เชื่อมต่อเซิร์ฟเวอร์ — รายการบนจออาจไม่ใช่ของล่าสุด</span>
                    </div>
                </header>

                <main id="board" aria-live="polite">
                    <div class="empty">กำลังโหลดรายการครัว…</div>
                </main>

                <div class="k-toasts" id="toasts"></div>

                <!-- แจ้งปัญหากลับหน้าร้าน: ของหมด / ล่าช้า / ขอเปลี่ยน / อื่น ๆ -->
                <div class="sheet-bg" id="issue-bg" role="dialog" aria-modal="true" aria-label="แจ้งปัญหาให้หน้าร้าน">
                    <div class="sheet">
                        <h3>แจ้งปัญหาให้หน้าร้าน</h3>
                        <div class="who" id="issue-target">—</div>
                        <div class="opts">
                            <button type="button" class="opt" data-issue="out_of_stock"><span class="ic">🚫</span>ของหมด</button>
                            <button type="button" class="opt" data-issue="delay"><span class="ic">⏱</span>ล่าช้า</button>
                            <button type="button" class="opt" data-issue="substitute"><span class="ic">🔁</span>ขอเปลี่ยนรายการ</button>
                            <button type="button" class="opt" data-issue="other"><span class="ic">💬</span>อื่น ๆ</button>
                        </div>
                        <input type="text" class="note-in" id="issue-note"
                               maxlength="250" placeholder="รายละเอียดเพิ่มเติม (ไม่บังคับ) เช่น เหลือ 1 ที่, รออีก 10 นาที"/>
                        <div class="sheet-act">
                            <button type="button" class="b-cancel" id="issue-cancel">ยกเลิก</button>
                            <button type="button" class="b-send" id="issue-send" disabled="disabled">ส่งให้หน้าร้าน</button>
                        </div>
                    </div>
                </div>

                <nav t-if="pos_config" class="k-nav" aria-label="เมนูหลัก">
                    <a t-att-href="'/pos/ui/%s' % pos_config.id">ขาย<small>Sell</small></a>
                    <a t-att-href="'/pos/ui/%s/ticket' % pos_config.id">บิล<small>Orders</small></a>
                    <a t-att-href="'/kds/pos/%s' % pos_config.id" class="on">ครัว<small>Kitchen</small></a>
                </nav>

                <script>
                    window.KDS_CONFIG_ID = <t t-esc="pos_config.id if pos_config else 0"/>;
                    window.KDS_STATION_ID = <t t-esc="station.id if station else 0"/>;
                    window.KDS_HAS_STATION_FILTER = <t t-esc="'true' if station else 'false'"/>;
                    window.KDS_CSRF = "<t t-esc="csrf_token"/>";
                </script>
                <!-- This script is served directly rather than through an Odoo asset
                     bundle. Keep a version query so browsers cannot reuse an older KDS
                     runtime after an addon upgrade. -->
                <script src="/ko_pos_kds/static/src/kds/kds.js?v=19.0.8.0.0"/>
            </body>
        </html>
    </template>

    <template id="kds_choose_page" name="KDS — เลือกร้าน">
        <html>
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1"/>
                <title>เลือกร้าน · KO KDS</title>
                <style>
                    :root {
                        --ko-teal: #0d7d88;
                        --ko-teal-dark: #08616a;
                        --ko-teal-soft: #e4f2f3;
                        --ko-ink: #101a1c;
                        --ko-ink-3: #5d7175;
                        --ko-border: #cfdcdb;
                        --ko-canvas: #eef3f3;
                    }
                    * { box-sizing: border-box; }
                    body {
                        margin: 0;
                        font-family: "Prompt", "Noto Sans Thai", system-ui, sans-serif;
                        background: var(--ko-canvas);
                        color: var(--ko-ink);
                        min-height: 100vh;
                        padding: 32px 20px;
                    }
                    .wrap { max-width: 800px; margin: 0 auto; }
                    h1 { font-size: 27px; margin: 0 0 6px; }
                    .sub { color: var(--ko-ink-3); font-size: 16px; margin: 0 0 26px; line-height: 1.5; }
                    .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(min(100%, 260px), 1fr)); gap: 14px; }
                    .shop {
                        display: block;
                        min-height: 120px;
                        text-decoration: none;
                        color: inherit;
                        background: #fff;
                        border: 2px solid var(--ko-border);
                        border-radius: 16px;
                        padding: 20px;
                    }
                    .shop:hover, .shop:active { border-color: var(--ko-teal); background: var(--ko-teal-soft); }
                    .shop-name { font-size: 21px; font-weight: 600; }
                    .shop-company { font-size: 15px; color: var(--ko-ink-3); margin-top: 3px; }
                    .shop-go { font-size: 16px; font-weight: 600; color: var(--ko-teal-dark); margin-top: 16px; }
                    .empty {
                        background: #fff;
                        border: 2px dashed var(--ko-border);
                        border-radius: 16px;
                        padding: 32px;
                        text-align: center;
                        color: var(--ko-ink-3);
                        font-size: 16px;
                        line-height: 1.5;
                    }
                </style>
            </head>
            <body>
                <div class="wrap">
                    <h1>เลือกร้านสำหรับจอครัว</h1>
                    <p class="sub">จอครัวแต่ละเครื่องจะแสดงเฉพาะออเดอร์ของร้านที่เลือก · บันทึกหน้านี้เป็นบุ๊กมาร์กของเครื่องนั้นได้เลย</p>
                    <div t-if="configs" class="grid">
                        <a t-foreach="configs" t-as="cfg" class="shop" t-att-href="'/kds/pos/%s' % cfg.id">
                            <div class="shop-name" t-esc="cfg.display_name"/>
                            <div t-if="multi_company" class="shop-company" t-esc="cfg.company_id.name"/>
                            <div class="shop-go">เปิดจอครัว →</div>
                        </a>
                    </div>
                    <div t-if="not configs" class="empty">
                        ยังไม่มีจุดขาย (POS) ที่คุณเข้าถึงได้ — ให้ผู้ดูแลระบบสร้างจุดขายหรือเพิ่มสิทธิ์บริษัทก่อน
                    </div>
                </div>
            </body>
        </html>
    </template>
</odoo>
